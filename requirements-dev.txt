# Development-only dependencies (linting, type checking, testing).
# Install after requirements.txt:
#     .venv/Scripts/python -m pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
httpx>=0.27          # FastAPI TestClient transport
ruff>=0.6
mypy>=1.11
