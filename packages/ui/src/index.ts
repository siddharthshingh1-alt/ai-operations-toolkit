/**
 * Shared UI components.
 *
 * Anything used by more than one project belongs here. CLAUDE.md Section 21:
 * this should look like an internal operations platform a serious startup
 * actually uses — clean and plain, not a flashy AI demo.
 */

export { Badge } from "./badge";
export type { BadgeTone } from "./badge";
export { Card, CardHeader } from "./card";
export { EmptyState } from "./empty-state";
export { StatusDot } from "./status-dot";
export { cn } from "./cn";
